==================
Comments
==================

// Comment A
  ; Comment B

PRINT "A";
ECHO "B" // a\
            b

---

(script
  (comment)
  (comment)
  (command_expression
    (identifier)
    (argument_list
      (literal))
    (comment))
  (command_expression
    (identifier)
    (argument_list
      (literal))
    (comment)))

==================
Interjected comments
==================

IF &a
 // Comment here
(
  PRINT "true"
)
ELSE
// Comment there
  PRINT "false"

---

(script
  (if_block
    command: (identifier)
    condition: (identifier)
    (comment)
    (command_expression
      command: (identifier)
      arguments: (argument_list
        (literal)))
    (else_block
      (comment)
      (command_expression
        command: (identifier)
        arguments: (argument_list
          (literal))))))
