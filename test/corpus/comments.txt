==================
Comments
==================

// Comment A
  ; Comment B

PRINT "A";
ECHO "B" // a\
            b

---

(script
  (comment)
  (comment)
  (command_expression
    (identifier)
    (argument_list
      (string))
    (comment))
  (command_expression
    (identifier)
    (argument_list
      (string))
    (comment)))

==================
Interjected comments
==================

IF &a
 // Comment here
(
  PRINT "true"
)
ELSE
// Comment there
  PRINT "false"

---

(script
  (if_block
    command: (identifier)
    condition: (macro)
    (comment)
    (block
      (command_expression
        command: (identifier)
        arguments: (argument_list
          (string))))
    (else_block
      command: (identifier)
      (comment)
      (command_expression
        command: (identifier)
        arguments: (argument_list
          (string))))))
